import React from 'react';
import { Button, View } from 'react-native';
const Application: React.FC = () => {
  return (
    <View>
      <Button title='Application' onPress={() => {}}/>
    </View>
  );
}

export default Application;
